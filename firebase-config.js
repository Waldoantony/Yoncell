// ============================================================
// Yoncell — Firebase configuration
// ============================================================
// Fill this in with YOUR Firebase project's values.
// Where to find them: Firebase Console → Project Settings (⚙️)
// → scroll to "Your apps" → select the web app → "SDK setup and
// configuration" → "Config".
//
// Both index.html and admin.html import this one file, so you
// only ever need to edit your keys in ONE place.
// ============================================================

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
